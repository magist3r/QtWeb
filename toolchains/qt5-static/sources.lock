# This lock file is sourced by build-qt5-static.sh.
# Every source must have at least one checksum (sha256 preferred, md5 fallback).

LOCK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# shellcheck disable=SC1091
source "${LOCK_DIR}/sources-qt.lock"
# shellcheck disable=SC1091
source "${LOCK_DIR}/sources-qtwebkit.lock"

unset LOCK_DIR
